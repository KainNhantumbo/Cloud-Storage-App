import { FC } from 'react';
import { HomeContainer as Container } from '../styles/home';

const Home: FC = () => {
	return (
		<Container>
			<div>Hello Kain</div>
		</Container>
	);
};

export default Home;
